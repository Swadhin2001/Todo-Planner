export type Task = {
    id: string;
    taskName: string;
    check: boolean;
    editCheck: boolean;
    isCompleted: boolean;
}

export type completedCss = {
    fontWeight : string;
    fontColor: string;
}
